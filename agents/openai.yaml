interface:
  display_name: "Dynamic Workflows"
  short_description: "Orchestrate or run bounded Codex workflows"
  default_prompt: "Use $dynamic-workflows to choose native subagent orchestration or, only when explicitly requested, a persisted codex-dw workflow for this task."

policy:
  allow_implicit_invocation: true
